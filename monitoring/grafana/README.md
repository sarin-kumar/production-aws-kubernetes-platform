# Grafana

Grafana should consume Prometheus as its data source.

Suggested dashboards:

- Kubernetes cluster health
- Node CPU and memory
- Pod restarts
- Container CPU/memory
- Deployment replica health
- HTTP request/error rate
